#Label {
    color: rgb(209, 209, 209);
    font-size: 25px;
    white-space: pre-wrap;
    -unity-text-align: upper-center;
}

.fade-label {
    opacity: 0;
    transition: opacity 1s ease-in;
}

.fade-label.show {
    opacity: 1;
}
